%h1 Listing events

%table
  %thead
    %tr
      %th
      %th
      %th

  %tbody
    - @events.each do |event|
      %tr
        %td= link_to 'Show', event
        %td= link_to 'Edit', edit_scrapper_path(event)
        %td= link_to 'Destroy', event, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Scrapper', new_scrapper_path
